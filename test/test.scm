;;; test.scm

(import (scheme base)
        (scheme write)
        (gauche base)
        (maali))

(define (main)
  (display "maali loaded"))

(main)

;; Local Variables:
;; coding: utf-8
;; indent-tabs-mode: nil
;; End:
